"use client";

import { WithdrawsReviewPage } from "@/components/withdraws-review-page";

/** Withdrawal review — real Ops list (replaces former placeholder at this route). */
export default function AdminWithdrawsRoutePage() {
  return <WithdrawsReviewPage />;
}
