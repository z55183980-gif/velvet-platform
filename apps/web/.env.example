# 后台前端入口。默认 /admin；若要隐蔽入口可改为 /ops 或 /x7k2m
NEXT_PUBLIC_ADMIN_BASE_PATH=/admin

# API 入口（开发默认 http://localhost:4000/api/v1）
NEXT_PUBLIC_API_BASE=http://localhost:4000/api/v1

# 默认语言：vi | zh
NEXT_PUBLIC_DEFAULT_LOCALE=vi
