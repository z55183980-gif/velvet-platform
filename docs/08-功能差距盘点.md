# 08 · 功能差距盘点（v1 → v7 文档 → 实现）

> 本文档对照 docs/README.md / 00～07 全部描述与当前 `apps/web` + `services/api` 实现，梳理**未实现 / 实现不到位 / 文档描述但代码缺失**的功能点。
> 上一轮（07 §八「已修复」）已闭环的 P0 / P1 不再重复列入。本表新增 / 残留项按优先级整理。

## 0. 总结（v4 更新）

- 已识别缺口：**23 项**（P0–P2 清单项）
- 优先级分布：P0 = 4、P1 = 9、P2 = 10
- **累计已补**：P0 = 4/4、P1 = 9/9、P2 = 10/10（含管理子页运营面与 Sprint 附录）
- **本轮（v4）新增闭环**：
  - Admin 运营子页可用（内容 / 提现 SLA / Banner·分类·汇率 / 审计 / 对账）+ 关键文案 `t('admin.*')`
  - Sprint：推荐位 `sortWeight` 排序、Banner 定时失效 cron、CSV 导出、退款审批（TOPUP 走审批）
  - 埋点：登录 / 充值 / 解锁 / KYC / 提交剧集 + `POST /api/v1/events`
- **仍真正依赖外部 / 刻意不做**：真实支付渠道、Better Auth、真 HLS CDN、第三方分析 SaaS、生产级对账文件拉取

---

## 0.1 已闭环项（不再重复）

通过深度审阅源码，确认以下已闭环（P0-1 ~ P0-5、P1-1 ~ P1-10、P2-1、P2-3 ~ P2-5、P2-9 全部修复；P0-7 T+7 cron 已就位；P0-6 HLS 短时签名已实现；P0-1 admin 账号体系已实现）：

- ✅ Admin 账号 + JWT + Cookie（admin-auth.service.ts / admin-auth.controller.ts）
- ✅ HLS 短时签名（episodes.service.ts:42-58）
- ✅ T+7 cron + earningSettled 幂等（reconcile.service.ts）
- ✅ 对账 daily cron（reconcile.service.ts）
- ✅ KYC 拒绝 / 通过端点（admin.service.ts）
- ✅ webhook 鉴权 / provider 匹配 / 重复支付防护（payments.service.ts）
- ✅ TOPUP 自助退款禁止（wallet.service.ts）；**TOPUP 审批退款**见 v4
- ✅ 退款抢占幂等：`refundOrder` / `refundTopupByAdmin` 以 `paymentStatus=PAID→REFUNDED` 条件更新防并发双扣/双入账；审批已退订单仍对齐 `refundStatus=APPROVED`
- ✅ 收藏备注可清空（PATCH 传 `null`）；重复取消收藏不再递减 `favoriteCount`
- ✅ Banner 创建/更新校验 `endAt > startAt`
- ✅ 剧集价格非负 / 免费集不带价（creator.service.ts）
- ✅ Prisma 错误屏蔽 + KYC / 汇率 / progressSec / LIVE 防回退 / 重复审核 等

---

## P0 / P1 / P2 缺口清单

> 详见历史章节；**v2–v3 已全部闭环**。v4 补充的是文档曾标为「刻意推迟」的 Admin 运营面与 Sprint 附录。

| 缺口 | 状态 |
|------|------|
| #1–#4 P0 | ✅ v2 |
| #5–#13 P1 | ✅ v2/v3 |
| #14–#23 P2 | ✅ v2/v3（#15 admin i18n 在 v4 扩到子页标题） |

---

## 附：与 Sprint 4-8 的对账（v4）

| Sprint 目标 | 现状 |
|---|---|
| 退款审批流 | ✅ TOPUP：`refund-request` → admin 批准（扣回积分）/ 拒绝；EPISODE_UNLOCK 仍可自助 `refund` 或走工单；审计 `order.refund.*` |
| T+1 日切对账 cron | ✅ reconcile.service.ts |
| 推荐位排序 | ✅ `Drama.sortWeight`；admin 可调；`getFeatured` 按 weight→官方→发布时间 |
| 定时下线 banner | ✅ startAt/endAt 查询过滤 + 每小时 cron `deactivateExpiredBanners`；admin 可设期 |
| 数据导出 (CSV) | ✅ `GET /admin/exports/{orders,withdraws,reconciliations}.csv` + 前端下载按钮 |
| 站内信 | ✅ 缺口 21（v2） |
| 用户头像 | ✅ 缺口 13 |
| 主题切换 | ✅ 缺口 16 |

---

## 附：本轮（v4）新修复

| 项 | 修复要点 |
|------|---------|
| Admin 运营面 | content / withdraws / banners / categories / rates / audit / reconcile 均接真实 API；标题走 `t('admin.*')` |
| 推荐排序 | `dramas.service.getFeatured` orderBy `sortWeight desc` |
| Banner 定时下线 | reconcile cron 过期 `isActive=false` |
| CSV | `AdminExportService` + 三端点；订单/提现/对账页导出 |
| 退款审批 | TOPUP 可申请工单；`refundTopupByAdmin` 扣积分；admin 订单页退款 tab |
| 埋点 | track 缓冲 + `POST /api/v1/events`；登录/注册/解锁/KYC/提交剧集 |
| 用户侧 | `/me` 订单 tab 可申请退款 |

### 新增 / 主要修改文件（v4）

- `services/api/src/admin/export.service.ts`
- `services/api/src/events/events.controller.ts` / `events.module.ts`
- `services/api/src/dramas/dramas.service.ts`（featured 排序）
- `services/api/src/reconcile/reconcile.service.ts`（banner cron）
- `services/api/src/wallet/wallet.service.ts`（`refundTopupByAdmin`）
- `services/api/src/admin/refund.service.ts` / `admin.controller.ts` / `admin.module.ts`
- `apps/web/lib/track.ts` / `api.ts` / `i18n.tsx`
- `apps/web/app/admin/orders|withdraws|reconcile|content|banners|…`
- `apps/web/app/me/page.tsx` / `creator/page.tsx` / `components/auth-context.tsx`

---

## 附：仍未完成（外部依赖 / 刻意范围外）

| 项 | 原因 |
|---|---|
| 真实 Alipay / Stripe / MoMo 等生产支付 | 需商户号、密钥、回调域名；现有 simulate / mock webhook |
| Better Auth 全量替换 | 文档可选方案；当前自建 JWT + OTP + 密码已可用 |
| 真 HLS CDN + DRM | 现为本地 MEDIA + 短时签名；CDN/DRM 需基础设施 |
| 渠道对账单 T+1 真文件比对 | cron 已写本地口径；remote 仍为 mock |
| 第三方埋点 SaaS（GA/Mixpanel） | 已有本地缓冲 + `/events` 结构化日志，未接 SaaS |
| 生产短信 / 邮件 OTP 供应商 | 开发态返回 `devCode`；SMTP 可选 |

---

## 附：历史轮次（v2 / v3）摘要

见本文件 git 历史与下方归档段落（v2 P0 审计/限流/KYC/日志；v3 creator 编辑、收藏、SLA、收益、头像、webhook 去重、主题、tag、续看、埋点骨架等）。不再逐条展开。
