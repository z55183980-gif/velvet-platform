{
  "name": "velvet-platform",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:web": "turbo run dev --filter=velvet-web",
    "dev:admin": "turbo run dev --filter=velvet-admin",
    "dev:api": "turbo run start:dev --filter=velvet-api",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "prisma:generate": "pnpm --filter velvet-api prisma:generate",
    "audit:prod": "pnpm audit --prod --registry=https://registry.npmjs.org",
    "audit:all": "pnpm audit --registry=https://registry.npmjs.org"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.7.3"
  }
}
